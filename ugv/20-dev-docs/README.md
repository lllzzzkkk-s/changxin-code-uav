# UGV Developer Docs

This folder tracks the OS-mate UGV interface inventory. Current state is manual-derived only.

Next source of truth is vehicle evidence generated by:

```bash
bash ugv/01-scripts/probe_ugv_readonly.sh --workspace-dir ~/catkin_ws --pack-source
```

Keep raw vehicle evidence outside git unless explicitly reviewed.


# Optional server lane for simulation, replay, CI, and artifact comparison.
MISSION_PROFILE=server_sim
MODEL_PROVIDER=mock
MODEL_BASE_URL=
MODEL_NAME=
PLANNER_BACKEND=mock
PLATFORM_BACKEND=sim
MISSION_STATE_STORE=json
MISSION_ARTIFACT_ROOT=./runs/server_sim
ROS_MASTER_URI=
ROS_IP=
HARDWARE_APPROVAL_REQUIRED=false
ROS_GATEWAY_DISPATCH_SERVICE_TEMPLATE=/fleet/{platform_id}/gateway/dispatch
ROS_GATEWAY_DRY_RUN_SERVICE_TEMPLATE=/fleet/{platform_id}/gateway/dry_run
